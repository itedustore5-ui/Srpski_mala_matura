import type { SelectedText } from "./types";

// Други део збирке: одабрани текстови који служе као основа за низ задатака.
// Задатак се везује за текст преко `textKey`, а не преко редног броја текста —
// редни број се мења кад се текст дода или уклони, а кључ остаје.

/**
 * Ознака да тело текста још није преписано из збирке.
 *
 * Одломци другог дела су цели штампани табаци, па се не преписују истом
 * брзином као задаци. Уместо да текст ћутке недостаје, стоји ова ознака:
 * `verify:scoring` је пријављује, а ученик на екрану види јасну поруку уместо
 * празне странице. Кад се текст залепи, ознака једноставно нестане.
 */
export const ZA_LEPLJENJE = "[[ЗА ЛЕПЉЕЊЕ]]";

export const texts: SelectedText[] = [
  {
    key: "gospodja-ministarka",
    title: "Госпођа министарка (Чин други, 10. сцена)",
    author: "Бранислав Нушић",
    // Одломак стоји на странама 101–102 збирке. Сваку реплику уписати као
    // засебан елемент низа; празан стринг раздваја целине.
    body: [ZA_LEPLJENJE],
    note: "Лица: Живка, Васа. Одломак: збирка, стране 101–102.",
  },
  {
    key: "andric-svet-knjige",
    title: "Како сам улазио у свет књиге и књижевности",
    author: "Иво Андрић",
    // Одломак стоји на странама 110–111 збирке. Сваки пасус као засебан
    // елемент низа.
    body: [ZA_LEPLJENJE],
    note: "Одломак: збирка, стране 110–111.",
  },
  {
    key: "dnevnik-ane-frank",
    title: "Дневник Ане Франк (четвртак, 2. март 1944)",
    author: "Ана Франк",
    // Одломак стоји на страни 114 збирке. Пасуси су у збирци обележени
    // бројевима 1–5 са леве стране — на то се позива задатак 250, па бројеве
    // треба задржати на почетку пасуса.
    body: [ZA_LEPLJENJE],
    note: "Одломак: збирка, страна 114. Пасуси су нумерисани 1–5.",
  },
];

const byKey = new Map(texts.map((t) => [t.key, t]));

export const textByKey = (key: string): SelectedText | undefined => byKey.get(key);

/** Текстови чије тело још чека да се препише. */
export const textsAwaitingBody = (): SelectedText[] =>
  texts.filter((t) => t.body.some((line) => line.includes(ZA_LEPLJENJE)));
